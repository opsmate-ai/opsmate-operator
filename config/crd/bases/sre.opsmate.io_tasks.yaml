---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: tasks.sre.opsmate.io
spec:
  group: sre.opsmate.io
  names:
    kind: Task
    listKind: TaskList
    plural: tasks
    singular: task
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.userID
      name: UserID
      type: string
    - jsonPath: .spec.environmentBuildName
      name: EnvironmentBuild
      type: string
    - jsonPath: .spec.instruction
      name: Instruction
      type: string
    - jsonPath: .status.output
      name: Output
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.reason
      name: Reason
      type: string
    - jsonPath: .status.internalIP
      name: InternalIP
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Task is the Schema for the tasks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TaskSpec defines the desired state of Task.
            properties:
              context:
                description: Context is the execution context for the task
                type: string
              environmentBuildName:
                description: The environmentBuild that the task will use, must be
                  in the same namespace as the task
                type: string
              instruction:
                description: Instruction is the instruction for the task
                type: string
              userID:
                default: anonymous
                description: The user ID that initiated the Task
                type: string
            required:
            - context
            - environmentBuildName
            - instruction
            - userID
            type: object
          status:
            description: TaskStatus defines the observed state of Task.
            properties:
              internalIP:
                description: Internal IP of the Task Pod
                type: string
              output:
                description: Output of the task
                type: string
              reason:
                description: Reason for the error
                type: string
              state:
                enum:
                - PENDING
                - SCHEDULED
                - RUNNING
                - TERMINATING
                - ERROR
                - NOT_FOUND
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
